# In order to make a vector [which is basically a list], 
  # put a c in front the parens that contain the elements.
a = c(1, 2, 3, 4, 5, 8)
# display individual elements
a[3]
# display the whole vector
a[1:length(a)]
# display a subset of the vector
a[2:5]

ls()
